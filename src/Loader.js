import React from "react";

const Loader = () => (
  <div style={{ dislay: "flex", justifyContent: "center", margin: ".5rem" }}>
    <div className="loader">Loading...</div>
  </div>
);

export default Loader;
